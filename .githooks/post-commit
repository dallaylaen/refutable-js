#!/bin/sh

AUTOFILES="docs/js/refute.js"

if git status --porcelain -- $AUTOFILES | grep '^'; then
    # TODO some checking
    git add $AUTOFILES
    git commit $AUTOFILES -m "[auto] Update autogenerated $AUTOFILES" --no-verify
fi


